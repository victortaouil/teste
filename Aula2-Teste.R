# Fazendo qualquer coisa em R pra ver como que funciona no Super GITHUUB. 

y <- function(x) {
    j <- x**2
    return(j)
}

x<-y(2)
x

# Fazendo um segundo commit para ver como funciona quando fechamos o arquivo e abrimos novamente

f <- c(1,2,3,4)

for (x in f) {
  print(x)
}
